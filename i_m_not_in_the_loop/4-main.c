#include "my_functions.h"

int main(void)
{
  positive_or_not(98); /*this is how the values to test are pushed to the function */
  positive_or_not(0);
  positive_or_not(402);
  positive_or_not(-459);
  print_char('\n'); /* new line */
  return (0);
}
